interface Student {
  firstName: string;
  lastName: string;
  age: number;
  location: string;
}

const student1: Student = new Student('Oscar', 'Angel', 33, 'Cartagena');
const student2: Student = new Student('Juancho', 'Polo', 30, 'Bogota');

const studentList: any[] = [user1, user2];

